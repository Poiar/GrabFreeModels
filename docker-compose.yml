services:
  api:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: grabfreemodels-api
    env_file:
      - .env
    ports:
      - "3001:3001"
    volumes:
      - ./server:/app/server
