# Gitleaks Install

**Windows (winget):**
```bash
winget install gitleaks.gitleaks
```

If winget is unavailable, grab the latest release from https://github.com/gitleaks/gitleaks/releases and add the binary to PATH.
